<% include partials/header %>

<!-- <div style="margin: 30px 30px">
	<div class="containter">
		<div class="row">
			<div class="col-md-4 col-6">
               <img src="/<%= event.bgImage %>"; style="height: 100%; width: 90%;">
			</div>
			<div class="col-md-4 col-6">
				<h2>Booked Ticket for <%= event.tourName %></h2>
				<h3><strong>Date:</strong> <%= event.tourDates[order.tourIndex] %></h3>
				<h3><strong>City:</strong> <%= event.tourCity[order.tourIndex] %></h3>
				<h3><strong>Country:</strong> <%= event.tourCountry[order.tourIndex] %></h3>
				<h3><strong>Venue:</strong> <%= event.tourVenue[order.tourIndex] %></h3>
			</div>
			<div class="col-md-2 col-12">
				<h4>Uploaded Sketch</h4>
				<img src="<%= event.bgImage %>"; style="height: 80%; width: 90%;">
			</div>
            <div class="col-md-2 col-12">
				<h4>Uploaded Cover</h4>
				<video src="../hotel.mp4" controls style="width: 80%; height: 90%"></video>
			</div>			
		</div>
	</div>
</div>	 -->

<!-- orders.forEach(function(order)){
	order.name,
	order.bgImage,
	order.tourDate[order.tourIndex],
	order.tourCity[order.tourIndex],
	order.tourCountry[order.tourIndex]
}) -->
<div style="height: 60%;"></div>



<% orders.forEach(function(order){ %>
	<%= order.name %>
	<%= order.bgImage %>
	<%= order.tourDate[order.tourIndex] %>
	<%= order.tourCity[order.tourIndex] %>
	<%= order.tourCountry[order.tourIndex] %>
<% }) %>

<% include partials/footer %>

